import { createRouter } from 'next/router'
import { generateRouteWithRequiredUrlParams } from '.'

describe('generateRouteWithRequiredUrlParams', () => {
  test('it maintains the required params when generating navigation urls', () => {})
})
